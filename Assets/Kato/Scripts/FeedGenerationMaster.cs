using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FeedGenerationMaster : FeedgenerationMasterRow
{
    

    // Start is called before the first frame update
    void Start()
    {
        var Group = new List<(int level, int maxFeeds, int repopNum)>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
